#!/usr/bin/env bash
# Fail the commit if Zeitwerk autoload naming is broken (file <-> constant mismatch).
# Activated via: git config core.hooksPath .githooks  (run by bin/setup).
set -e

echo "pre-commit: zeitwerk:check…"
bin/rails zeitwerk:check
